name: Uptime Monitor

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Check health endpoint
        run: |
          curl --fail https://Semyon-MLOps-iris-api-voronin.hf.space/health || echo "Health check failed"